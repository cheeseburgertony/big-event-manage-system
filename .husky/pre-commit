#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# npm test
# 默认进行的是全量检查，耗时问题，历史问题(无论是在仓库中的还是不在仓库中的都会进行检查校验，很耗费事件，而且拉取别人的代码如果不符合本地的ESLint规范，又不想修改还提交不上去)
# pnpm lint

# 所以使用 lint-staged 只对暂存区的代码进行校验，如果你拉取别人的代码，你没有修改原本那些不符合规范的代码，就不会上传到暂存区
# 所以这个只会对你在本地操作如何提交到暂存区的代码进行检验然后符合条件的才提交到仓库 并且这个相对耗时较短
pnpm lint-staged